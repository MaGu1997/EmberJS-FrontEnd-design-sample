<div class='container'>
  <div class='container__text'>
    <h1>{{@onlineProjects.length}} projects online </h1>
  </div>
  {{#each @onlineProjects as |project|}}
    <OnlineCard @project={{project}} />
  {{/each}}
</div>